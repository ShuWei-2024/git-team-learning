## fuxindong 

Try to push 

## fujiajun
resolve a conflict

## xiongyf

1.  `git push`：把本地提交同步到远程，首次推送个人分支可用 `git push -u origin xiongyf`，之后直接 `git push` 即可。

2. 在本地制造了冲突并解决：打开文件删掉 `<<<<<<<` 等标记，保留正确内容后 `git add` 再 `git commit`.


